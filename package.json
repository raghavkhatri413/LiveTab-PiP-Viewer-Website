{
  "dependencies": {
    "firebase": "^11.5.0"
  },
  "devDependencies": {
    "vite": "^6.2.3"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy:functions": "firebase deploy --only functions"
  }
}